{
  "tasks": [
    {
      "id": "fix-add-bug",
      "description": "The add() function returns a-b instead of a+b. Fix it.",
      "workspace": "./examples/workspaces/fix-add-bug",
      "setup_command": null,
      "verify_commands": [
        "python -m pytest -q test_add.py"
      ],
      "timeout_s": 120,
      "metadata": { "language": "python", "difficulty": "trivial" }
    }
  ]
}
